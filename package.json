{
  "name": "strikeoutlab",
  "private": true,
  "version": "0.2.0",
  "description": "StrikeoutLab: sistema determinista de análisis de props de ponches MLB. Solo Supabase + repo, sin app — se usa en vivo desde una sesión de Claude Code.",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspace packages/core",
    "build": "npm run build --workspace packages/core",
    "postinstall": "npm run build --workspace packages/core"
  },
  "engines": {
    "node": ">=20"
  }
}
